## 2026-09-12 — Reconciliation as a role
Codified how the repo converges: a reconciliation-terminal charter lives in the repo, and CLAUDE.md now carries the git conventions — one live session per worktree, the main checkout never on a feature branch, shared namespaces single-writer, stop point to PR the same hour. The failure it answers was sessions colliding on one checkout, not branches failing to merge.

## 2026-09-12 — Telemetry that cannot conflict
The build-session Stop hook now writes one shard per session instead of appending every worktree to a single tracked ledger. The old file was the one guaranteed merge conflict and fourteen churn commits a month; the legacy ledger is frozen history and readers read both.

## 2026-09-12 — A decision record that shows its own wrong claim
The build-quality judge flagged ADR-8 Increment 1 for claiming a scope it had not held, and the fix struck the false claim in place rather than rewriting it, deleted the unconsumed graph edges, and added a self-test so the extractor cannot regress silently. The rebuild went from about five seconds to a fifth of one.
